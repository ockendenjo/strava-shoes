name: pr

on:
  pull_request: {}

jobs:
  test:
    name: test
    uses: ./.github/workflows/test.yaml

  build:
    name: build
    uses: ./.github/workflows/build.yaml

  synth:
    name: synth
    needs: [build]
    uses: ./.github/workflows/synth.yaml
