name: pr

on:
  pull_request: {}

jobs:
  test:
    uses: ./.github/workflows/test.yaml

  build:
    uses: ./.github/workflows/build.yaml

  plan:
    needs: [build]
    uses: ./.github/workflows/plan.yaml
